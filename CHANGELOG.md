# 变更记录

## 2026-08-13 小程序端接入真实 AI

- 小程序端复制 `aiChat` 云函数（智谱 GLM-4-Flash-250414），与网页端共用同一 AI 服务，支持本地运行调试；API Key 存放于 `config.local.json` 并在 `.gitignore` 中忽略。
- 网页端和小程序端 AI 助手移除 mock 问答，改为真实调用 `aiChat.chat`，保留对话上下文；AI 回答按 Markdown 排版渲染，思考中显示实时秒数。
- 网页端和小程序端视频详情页课程简介改为 AI 生成，内容为「课程概述」「你将学到」两部分，带本地缓存（版本号 v3）避免重复请求，生成中显示加载提示与「AI 生成」标记。
- 网页端视频详情页课程简介精简为「课程概述」「你将学到」两个部分，移除「适合人群」「课程亮点」；简介缓存升级版本号，旧缓存自动作废并重新生成。
- 网页端视频播放器卡片固定自身高度（16:9 视频 + 控制条），不再随课程简介卡片内容增高而被拉伸。
- 网页端课程简介卡片最高与播放器等高，内容超高时在卡片内部滚动查看；窗口缩放、字体加载完成后高度自动联动。

## 2026-08-12 资源导入、视频列表与 B站播放支持

- 管理端“录入学习资源”新增视频分类/资源类型字段，并明确封面 URL 用途。
- 资源编辑弹窗支持一次修改标题、分类、时长/难度、地址、封面、日期、简介和审核状态。
- `resources` 云对象允许更新 `date`，并增加审核状态合法性校验；网页端与小程序本地副本同步。
- 学习中心视频学习只展示最新 4 个视频，“更多视频”接入新的全部视频页。
- 新增网页端 `video-list` 全部视频页，支持搜索、分类筛选并跳转视频详情。
- “为你推荐”改为只推荐 4 个视频，新增刷新按钮，按随机替换逻辑更换推荐内容。
- 学习中心首页和全部视频页支持读取资源封面 URL，未填封面时保留渐变占位。
- 网页端视频详情支持 B站播放页地址和官方嵌入地址，自动切换为 iframe 播放。
- 修复直接打开视频详情页时资源未加载导致的空数据渲染报错。
- 重写资源库数据导入操作指南，补充云存储上传、URL 复制、管理端导入、上线验证、封面和 B站处理流程。

## 2026-08-09 静态数据与小程序真实数据收口

- 小程序测评接入 `questions.listPublic`，移除硬编码题目。
- 小程序交卷改为真实计分，并通过 `survey.saveResult` 保存测评结果。
- `survey` 云对象新增 `time` 字段，`survey_result.schema.json` 同步补充。
- 小程序数据中心、个人中心从 `survey.myResults` 读取真实测评记录。
- 小程序首页推荐、法律英语资源改为读取真实 `resources`。
- 小程序成就页、学习进度、听力训练等暂无数据模型，已清空为待接入状态。
- 网页端和小程序端视频详情移除默认示例视频回退，未配置 `fileUrl` 时提示缺少文件地址。
- 网页端专项选择页清空静态专项分类，待后续接入题目分类。
- 移除 `questions`、`resources`、`knowledge` 云对象中的自动种子数据。
- 修复题目库主观题 `subType` 丢失问题，并补上“论述题 / 案例分析题”切换。
- 修复问卷页题干字段读取错误，改为读取题目库的 `title`。

## 2026-08-08 云端框架与管理端

- 新增并完善 `user`、`resource`、`survey_result`、`question`、`legal_doc` 数据库 Schema。
- 新增并完善 `users`、`resources`、`survey`、`questions`、`knowledge` 云对象。
- 管理端题库、知识库、资源库、用户管理、数据总览接入真实数据。
- 网页端首页、登录、测评、结果、报告、个人中心、学习中心、法律库接入真实云数据。
- 小程序本地 `cloudfunctions` 同步 `users`、`resources`、`survey`、`questions`、`knowledge` 副本。

## 2026-08-06 初始版本

- 从原项目同步基础版本。
- 建立网页端与微信小程序端双端项目结构。
- 保留 uniCloud 云函数、数据库 Schema、页面与本地调试脚本。
